import React from "react";
import List from "./List";

function onNull({ onNo }) {
  if (activeDatas.length !== 0) {
    <NoteList key={contact.id} id={contact.id} onDelete={onDelete} />;
  } else {
    <p className="contact-list">Tidak Ada Catatan</p>;
  }
}

export default onNull;
